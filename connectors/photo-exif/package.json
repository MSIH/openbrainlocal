{
  "name": "life-context-connector-photo-exif",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Batch-scans a photo archive for EXIF date/GPS and syncs to LifeContext as photo artifacts, plus separate low-priority workers: a VLM caption worker and a local face-detection worker that both enrich the artifacts in place.",
  "main": "scan.js",
  "scripts": {
    "test": "node --test test.mjs"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.22.0",
    "@vladmandic/face-api": "^1.7.15",
    "canvas": "^3.1.0",
    "exifr": "^7.1.3"
  },
  "devDependencies": {
    "piexifjs": "^1.0.6"
  },
  "engines": {
    "node": ">=18.12.0"
  }
}
