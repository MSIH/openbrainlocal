{
  "name": "life-context-connector-photo-exif",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Batch-scans a photo archive for EXIF date/GPS and syncs to LifeContext as photo artifacts, plus a separate low-priority VLM caption worker that enriches them in place.",
  "main": "scan.js",
  "scripts": {
    "test": "node --test test.mjs"
  },
  "dependencies": {
    "exifr": "^7.1.3"
  },
  "devDependencies": {
    "piexifjs": "^1.0.6"
  },
  "engines": {
    "node": ">=18"
  }
}
