# Copy to .env and fill in. The server hard-exits if BRAIN_SECRET_KEY is unset
# or left at this placeholder value.

# Required — a long, random secret. Clients send it as the x-api-key header.
BRAIN_SECRET_KEY=change-this-to-a-long-secure-token

# Optional — defaults to 3000.
PORT=3000
